<h1>Listing games</h1>

<table>
  <thead>
    <tr>
      <th>Player home</th>
      <th>Player away</th>
      <th>Home score</th>
      <th>Home score</th>
      <th>Away score</th>
      <th>Away score</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= game.player_home_id %></td>
        <td><%= game.player_away_id %></td>
        <td><%= game.home_score %></td>
        <td><%= game.home_score %></td>
        <td><%= game.away_score %></td>
        <td><%= game.away_score %></td>
        <td><%= link_to 'Show', game %></td>
        <td><%= link_to 'Edit', edit_game_path(game) %></td>
        <td><%= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game', new_game_path %>
